using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LightOverlay : MonoBehaviour
{
    /*
     * The plan for this class is to use CustomLightSource.Mesh as a mask
     * That way, a sprite object will have two sprites
     * One that's lit up, and the other that's dark
     * It'll prolly check all CustomLightSources in the scene, might have to have it check by distance for performance
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
